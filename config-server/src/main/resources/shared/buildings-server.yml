spring:
  devtools:
    restart:
      enabled: true
  data:
    mongodb:
      host: localhost #should be localhost (without Docker)
      port: 27017
      username: mrlanu
      password: mrlanu
      database: warmsnow
  application:
    name: buildings-server

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance-id:${random.value}}

server:
  servlet:
    context-path: /buildings
  port: ${PORT:0}
